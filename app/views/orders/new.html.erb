<h1>New Order</h1>

<%= form_for @order, :url=>{:action=>"create",:uid=>@user.id},:html => { :multipart => true } do |form| %>

<%= render partial: 'form' ,locals: {f: form}%>
<% end %>
<br>
<%= link_to 'Back', orders_path , class:'btn btn-block btn-info col-xs-12 col-md-12'%>
   